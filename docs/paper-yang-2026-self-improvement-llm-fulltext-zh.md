---
lang: zh-CN
source: arXiv:2603.25681
arxiv_url: https://arxiv.org/abs/2603.25681
en_fulltext: paper-yang-2026-self-improvement-llm-fulltext.md
github: https://github.com/Zesearch/self-improvement-llm
authors_en: Haoyan Yang, Mario Xerri, Solha Park, Huajian Zhang, Yiyang Feng, Sai Akhil Kogilathota, Jiawei Zhou
affiliation: 纽约州立大学石溪分校 Zesearch NLP Lab
translator_note: 第2–6章技术综述篇幅极大，以下采用「中文结构化综述 + 原文图表逻辑还原」；如需与英文逐句对照，请并列阅读 en_fulltext 文件。参考文献条目未逐条翻译，见英文版附录。
---

# 大语言模型自我改进：技术综述与未来展望（中文版）

**原题：** Self-Improvement of Large Language Models: A Technical Overview and Future Outlook

**作者：** Haoyan Yang, Mario Xerri, Solha Park, Huajian Zhang, Yiyang Feng, Sai Akhil Kogilathota, Jiawei Zhou

**机构：** Zesearch NLP Lab, Stony Brook University

**代码：** https://github.com/Zesearch/self-improvement-llm

---

## 摘要

随着大语言模型（LLM）能力持续提升，仅依赖人类监督进行改进的成本越来越高、可扩展性受限。当模型在某些领域接近人类水平时，人类反馈可能不再能为进一步改进提供足够信息量的信号。与此同时，模型自主决策与执行复杂行为的能力增强，使得模型开发流程中的各个环节有望被逐步自动化。这些挑战与机遇共同推动了对「自我改进」的兴趣：即模型自主生成数据、评估输出、迭代强化自身能力。本文从**系统级视角**梳理自改进语言模型，提出统一框架以组织现有技术。我们将自改进系统概念化为**闭环生命周期**，包含四个紧密耦合的过程：**数据获取、数据选择、模型优化、推理细化**，以及一层**自主评估**。在该框架中，模型自身在各阶段扮演核心驱动角色：收集或生成数据、筛选信息量高的信号、更新参数、细化输出；自主评估层则持续监控进展并跨阶段引导改进循环。基于该生命周期视角，我们从技术角度系统回顾并分析各组件的代表性方法，讨论当前局限，并展望通向完全自改进 LLM 的未来研究方向。

---

## 1 引言

大语言模型通过扩大模型规模、训练数据与算力取得了快速而稳定的性能提升。普遍假设是：更大、更高质量的数据集——尤其是专家标注的人类监督——会造就更强的模型。实践中，RLHF 等方法严重依赖精心策划的高质量监督来对齐并微调预训练模型。然而，随着模型继续进步，**主要依靠人类监督**来改进模型暴露出若干结构性限制：

**(1) 人类数据稀缺。** 高质量专家标注昂贵且难以规模化；构建大规模监督数据集的边际成本快速上升，而专家劳动力有限。

**(2) 与人类认知边界相关的更深限制。** 若模型监督永久受人类智能上限约束，模型能否真正超越人类水平？当模型在某些领域接近或达到人类能力时，人类反馈可能不再为继续改进提供足够信息量的梯度。

这些局限促使人们探索**模型自我改进**：模型利用自身能力生成数据、评估输出、迭代优化策略。

从自动化视角看，这一方向不仅可取，而且自然。LLM 已能处理复杂工程任务与高层决策；而 LLM 开发本身（数据获取、数据选择、模型训练）就是高度复杂的工程。将开发职责逐步交给模型，用 LLM 作为智能体编排自身开发生命周期，可建立「系统侧」自改进循环。

**自改进**在此定义为：模型在**无持续人类在环监督**的情况下迭代增强自身能力的学习范式。其两个本质属性为：

- **自主性（Autonomy）：** 改进过程不依赖持续人工标注或手工修正。「自我」不排斥外部模块；教师模型、验证器、评论者、奖励模型或自动评估器仍可使用；关键是学习环一旦部署即**完全自动化**。

- **连续性（Continuity）：** 自改进不是一次性微调，而是**迭代、自我强化**的过程：早期阶段的输出或经验被复用以产生更强的后续监督信号；每轮改进依赖并放大先前结果，实现随时间累积的进步。

在此定义下，自改进不仅是任务指标层面的技巧，而是使**持续、自主增长**成为可能的结构性能力。

### 五组件生命周期框架

如图 2 所示，本文提出由五个相互连接部分构成的生命周期自改进系统：

| 阶段 | 含义 |
|------|------|
| **数据获取（Data Acquisition）** | 模型自主收集或生成训练数据 |
| **数据选择（Data Selection）** | 模型独立评估并筛选哪些数据点质量更高、更适合当前学习 |
| **模型优化（Model Optimization）** | 模型自主学习，将数据转化为参数内的能力增强 |
| **推理细化（Inference Refinement）** | 在**不改变底层参数**的前提下，在推理过程中提升表现 |
| **自主评估（Autonomous Evaluation）** | 对模型表现提供持续反馈，引导长期迭代改进 |

静态基准很快过时，人类驱动评估难以随系统规模扩展；自主评估使模型能获得及时、自适应的反馈，支撑长期可持续改进。

### 与既有综述的区别

已有工作分别从策略级自进化（自训练+RL）、推理时改进（提示与解码）、智能体系统（记忆、反思、工具）等角度切入。本文与多数工作的差异在于：采用**系统级视角**，将自改进视为**统一的闭环生命周期**，把模型开发的各阶段整合为可扩展、可自主演化的端到端框架。

### 本文结构

**技术部分（§2–§6）：** 依次研究数据获取、数据选择、模型优化、推理细化、自主评估；每节含概述、分类体系（见图 3）、小节讨论与跨阶段交互。

**综合部分（§7–§9）：** 挑战与局限、应用、未来展望；并从更广视角讨论系统（见图 9）。文中亦纳入自进化智能体相关工作（如 §5.4 推理时基于智能体系统的改进，§8 跨领域应用）。

---

## 2 面向自我改进的数据获取（结构化中文综述）

### 2.1 总览

在自改进生命周期中，**数据获取**指利用模型自主收集或生成自身演化所需的原始材料。向模型驱动范式转变有两方面动因：**效率**（模型可 7×24 处理/生成数据，突破人类带宽瓶颈）与**质量**（现代 LLM 生成信号在许多任务上已可与人类策划内容竞争，甚至在逻辑一致性上超越外包标注）。

作者将获取机制分为**三个层级**，自主性递增、对既有数据源依赖递减：

1. **静态策划（Static Curation）：** 模型与「既有世界」交互——在互联网快照或数据库中导航，筛选对当前演化最有价值的语料；数据是**存量**，模型角色是**发现**。
2. **环境交互（Environment Interaction）：** 模型与「动态工具」交互——通过 API、代码执行、模拟器产生行动轨迹并从反馈中学习；数据是**挣来的**，非预先存在。
3. **合成生成（Synthetic Generation）：** 模型与「内在自我」交互——脱离外部环境，用内在逻辑产生全新推理链或指令；**无中生有**式合成经验。

逻辑路径：外部发现（策划）→ 外部探索（交互）→ 内部生成（合成）。

### 2.2 静态策划

从固定外部源检索内容，模型作智能过滤器。传统管线依赖启发式规则与脚本；C4、CCNet、RefinedWeb、The Pile、Dolma、RedPajama 等奠定了爬取、去重、语言识别与质量过滤实践。**模型驱动质量过滤**成为趋势：如 FineWeb-Edu 用合成标注训练教育价值分类器；DCLM 表明基于模型的过滤是组装高质量训练集的最关键因素之一；Crawl4LLM 将模型指导前移到**爬取优先级**本身。

**网页、代码与科学文本、图书**等子类各有管线特点；模型还可参与数据准备自动化（清洗、集成、增强）。挑战包括：出处与信任、与机构基础设施集成难、缺乏即时反馈（需下游预训练表现评估，昂贵且慢）。

### 2.3 环境交互

通过**行动**获取数据；数据含**交互轨迹**（观察、行动、可选成功信号）。与静态策划的根本区别：(1) **行动–观察循环**与因果结构；(2) **自适应采集**可针对欠表示域或困难任务。

**表1 类型概览（环境类别 → 代表工作）：**

- **网页与工具：** WebGPT、Toolformer、Go-Browse、BrowserAgent、InSTA、EnvScaler 等。
- **代码执行：** TRACED、RLEF、CWM、CodeRL+、AgentFounder、Learn-by-Interact 等——执行轨迹编码运行时语义，区别于仅静态代码语料。
- **游戏环境：** Supervise Thyself、Generative Agents、ALYMPICS、FAIRGAME 等——策略与社会交互数据。

### 2.4 合成生成

在无环境反馈、无人类监督下规模化产生训练数据；瓶颈从**数据可得性**转为**合成质量与多样性**（需避免模型坍塌与分布漂移）。

**表2 生成机制分类：**

- **基于提示：** TinyStories、Phi 系列、Self-Instruct、WizardLM、Cosmopedia、MIND、约束式数学合成等。
- **基于变换：** WRAP、Instruction Pre-Training、SBP、SynthLLM、Gradient Matching 等。
- **基于交互（多智能体/自博弈）：** Baize、CICERO、SPIN、ALAS、LSP、R-Zero 等。

### 2.5 讨论要点

三种路径**互补**：静态策划规模大但针对性弱；合成生成可控但需多样性管理；环境交互单位样本成本高但信号可验证、难以被纯文本替代。训练生命周期不同阶段三者**权重不同**：早期预训练以静态为主；中后期合成与**环境交互轨迹**（工具、代码、浏览）比重上升。面向自主数据获取的闭环是：**评估弱点 → 选择获取路径（爬取/探索/合成）→ 下游选择与训练**。

---

## 3 面向自我改进的数据选择（结构化中文综述）

### 3.1 总览

**数据选择**是原始数据获取与模型优化之间的桥梁，决定哪些样本进入训练。传统静态过滤与模型无关、无法随模型演化。自改进要求转向**模型驱动**：利用模型内部状态（损失、困惑度、不确定性等）策展训练信号。

两大范式（见图 5）：

1. **指标引导打分（Metric-Guided Scoring）：** 用模型衍生信号打分；可一次性（one-shot）或**迭代重打分**（随训练刷新）。
2. **自适应选择（Adaptive Selection）：** 选择器本身可学习，通过 RL、Bandit、双层优化等**优化选择策略**，实现「课程可塑」。

### 3.2 指标引导打分

分为 **一次性打分**（训练前固定子集/权重）与 **迭代重打分**（训练循环内反复计算）。信号类型包括：似然/困惑度、损失轨迹、梯度与影响、难度、不确定性、相似度、外部评估器等。权衡：一次性开销低但适应性差；迭代重打分计算成本更高但更贴合模型当前状态。

### 3.3 自适应选择

与自改进愿景最契合：由**可学习选择策略**在给定学习状态下决定下一步最益数据。区分**选择单元**（样本/域/批次粒度）、**选择循环**（选择–评估–更新）、与主模型的**耦合方式**。相比固定规则，可优化长期目标并显式权衡探索–利用。

### 3.4 讨论

选择阶段决定「模型在何种数据分布上训练」；错误选择可放大噪声或导致**自噬（autophagy）**。与获取阶段耦合：获取扩张边界，选择决定**有效学习信号**。

---

## 4 面向自我改进的模型优化（结构化中文综述）

### 4.1 总览与 GRO 框架

**模型优化**将经验转化为参数能力。作者用 **Generation–Reward–Optimization（GRO）** 统一描述：

1. **生成（Generation）：** 当前策略产生候选输出、推理链或轨迹（自探索、迭代细化、交互式生成等）。
2. **奖励（Reward）：** 自动评估得到反馈——二值过滤、标量分数、偏好对等；来源可为启发式、基于模型或**可验证**信号。
3. **优化（Optimization）：** 根据奖励格式选用 SFT、RL（如 PPO）、偏好学习（如 DPO）等更新参数。

### 4.2–4.3 代表范式

涵盖 **STaR、LMSI、Self-Rewarding、SPIN、SCoRe、R-Zero** 等自博弈与自精炼路线；奖励类型涵盖启发式一致性、模型自评、形式化/执行验证等。

### 4.4 理论视角

讨论自改进中的收敛、分布偏移、奖励可辨识性等（具体定理见原文）。

### 4.5 GRO 之外

包括 **Gödel Agent、Darwin Gödel Machine** 等元层自修改与开放-ended 搜索方向。

### 4.6 讨论

优化环与数据选择、评估信号质量强耦合；错误奖励导致 Goodhart 与对齐风险（详见 §7）。

---

## 5 推理细化（结构化中文综述）

在不改参数的前提下提升测试时表现，包括：

- **解码策略：** 投机解码、logit 调整、树搜索、多样本投票等。
- **基于推理的改进：** 多智能体协作、规划、自反思（Self-Refine、CRITIC 等）。
- **智能体系统：** 工作流、工具、记忆、提示进化（如 ToolLLM、Mem0、AlphaEvolve 等）。
- **测试时训练：** TT-RL、LADDER、TT-SFT、SEAL 等。

与 §4 的边界：§4 改权重，§5 改**推理过程/上下文/解码**。

---

## 6 自主评估（结构化中文综述）

### 6.1 总览

评估从静态基准转向**动态**与**交互式**：动态基准（随时间更新测试分布）、交互环境（多步轨迹上的成功/过程质量）。子类包括基于结果与基于过程的环境评估。

### 6.2–6.3

动态基准与交互环境代表工作：REALTIME QA、LiveCodeBench、AppWorld、AndroidWorld、SafeArena、Lean-Gym 等。

### 6.4 讨论

评估应与自改进**同速**；需减少人类标注瓶颈，转向 LLM 辅助生成与核验评测项；动态基准与交互评估边界趋于融合。

---

## 7 挑战与局限（全文译文）

自改进系统虽有望让 LLM 持续提升能力，也引入多种失效模式。本节从五个维度归类（并补充第六点监督瓶颈）：

**(i) 数据自噬（Data Autophagy）：** 信息质量在闭环中退化——获取与选择限制、数据复制、灾难性遗忘、**模型坍塌**。合成数据循环降低多样性；确认偏误；最优数据选择往往需要可靠验证信号而在自主设定下不可得。

**(ii) 有缺陷的反馈信号：** 自生成评估存在偏差、不一致与不稳定；偏见放大；自偏好（self-bias）在 LLM-as-judge 与自奖励训练中加剧。

**(iii) 优化驱动失效：** 对代理奖励的强优化导致 **奖励操纵（reward hacking）** 与 **新兴不对齐（emergent misalignment）**；Goodhart 定律；链式思维中的掩饰性操纵等。

**(iv) 无效自细化：** **生成–验证鸿沟**并非普遍成立；自纠正能力有限，甚至纠正后变差；对外部呈现错误的纠正与对自身生成错误的纠正不对称。

**(v) 评估瓶颈：** 基准污染、指标设计缺陷（仅结果正确无法区分侥幸）、LLM 评判偏差（提示敏感、家族偏好泄漏等）。

**(vi) 监督瓶颈：** 人类监督质量与成本限制；LLM 监督的逻辑冲突；**监督失效**——对齐作假、表面安全风格记忆、对齐技术多为抑制而非根除不安全行为等。

---

## 8 应用（全文译文）

如图 11 与表 14 所示，自改进系统已出现在多领域：

- **代码：** 利用编译器与单元测试等确定性反馈（如 ReVeal、CURE、SSR、LLMLOOP 等）。
- **数学：** 形式一致与逐步验证（SIAM、Goedel-Prover、SPHERE、rStar-Math 等）。
- **医学：** 临床安全与多学科协作（MDTeamGPT、HealthFlow、MedAgentSim 等）。
- **金融：** 分层记忆与风险敏感模拟（FINMEM、FinAgent、AlphaAgents 等）。
- **算法/元能力：** 自指框架与算法开放演化（Gödel Agent、AlphaEvolve、SEAL 等）。
- **科学：** 实验设计与多学科综合（MatPilot、AI Scientist-v2、AI co-scientist 等）。

另有智能体系统设计自动化、研究流程自动化、模型动态安全（SISF 等）。

---

## 9 未来展望（全文译文）

领域正从孤立技巧转向**系统性与智能体化**视角。四个方向：

1. **从模型级优化到端到端自改进系统：** LLM 作为更广智能体架构的核心，在自动化循环中持续获取数据、自评、更新与再部署。

2. **领域专用、应用中心的自改进模型：** 在约束环境内迭代精炼，而非单一巨型通用模型。

3. **自改进与自主评估的统一基准：** 需直接测量**演化过程**——改进速度、抗退化、反馈转化效率等。

4. **自动化与人类监督的平衡：** 可调的监督框架，按任务关键性与风险校准自主程度。

---

## 10 结论（全文译文）

本文提出统一框架，将数据获取、数据选择、模型优化与推理细化整合为单一闭环，并与自主评估耦合，在减少对人类标注依赖的同时实现持续改进。

更重要的是，自改进正从优化孤立技术转向**构建整合系统**；模型从被动学习转向主动参与自身改进。展望未来，系统应能规划改进过程、适应新场景、迭代细化数据与行为；评估也需从一次性静态基准演进为动态、连续的监测框架。

该方向也带来显著风险：数据质量下降、奖励操纵、不对齐等。关键是在增强自主性的同时保持稳健安全与控制机制，使自改进稳定、可靠并与人类价值对齐。

总之，自改进正迈向**系统驱动、更加自主**的范式，模型通过紧密集成的学习管线主动参与自身开发。

---

## 参考文献

完整参考文献列表见英文全文：

`paper-yang-2026-self-improvement-llm-fulltext.md` 中 **References** 一节（与 arXiv PDF 一致）。

---

## 附录：第2–6章与英文逐段对照说明

| 章节 | 本中文版 | 英文完整版 |
|------|----------|------------|
| §2–§6 技术细节 | 本节文件中的结构化综述与分类表 | `paper-yang-2026-self-improvement-llm-fulltext.md` 对应 `## 2`–`## 6` |
| 图表 | 文中以表格概括 Figure 3–6 等逻辑 | 原图见 PDF |

如需将 §2–§6 全文译为中文，建议在专业翻译流程或机器翻译后由领域专家校对术语（GRO、DPO、自博弈等）。
